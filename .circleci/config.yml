version: 2
jobs:
  build:
    docker:
      - image: rgardler/simdem_cli:0.7.4-dev

    steps:
      - checkout
        
      - run:
          name: run hello world tests
          command: |
            simdem -p . test

      - run:
      	  name: run resource group tests
	  command: |
	    simdem -p ./incubator/azure_compute/resource_group --fastfail false test